(function(){
  // tiny enhancement: mobile nav toggle if menu isn't output by wp_nav_menu structure
  // (left simple so developers can extend)
  document.addEventListener('DOMContentLoaded',function(){
    // Intentionally minimal
  });
})();

